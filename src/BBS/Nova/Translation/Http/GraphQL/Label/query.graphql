extend type Query {
    "Fetch labels by locale ISO"
    labelsByIsos(
        "Locale filters"
        localeFilters: LocaleFilters
    ): [Label!]! @translation(model: "BBS\\Nova\\Translation\\Models\\Label")
}
